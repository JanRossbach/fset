(ns fset.backend.translation
  (:require
   [com.rpl.specter :as s]
   [clojure.core.matrix :as m]))

;; Translation from IR to matrix and back again

(defn ir->matrix
  [ir]
  ir)

(defn matrix->ir
  [matrix]
  matrix)
